# src/main/resources/application.prod.yml

spring:
  datasource:
    url: jdbc:mysql://158.180.66.149:3306/devjob?serverTimezone=Asia/Seoul&useSSL=false
    username: itsdev
    password: ${DB_PASSWORD}  # 환경변수 사용 권장
    
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
      
logging:
  level:
    "[kr.itsdev.devjobcollector]": INFO
    "[org.hibernate.SQL]": WARN
    
server:
  port: 8080